#include "main.h"
/**
 * _strcmp - Entry point
 *@s1: first array
 *@s2: second array
 * Return: Always (0) (Sucess)
 */
int _strcmp(char *s1, char *s2)
{
	int result = s1[0] - s2[0];

	return (result);
}
